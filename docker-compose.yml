services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: tidebase
      POSTGRES_PASSWORD: tidebase
      POSTGRES_DB: tidebase
    ports:
      - "7432:5432"
    volumes:
      - tidebase-postgres:/var/lib/postgresql/data

volumes:
  tidebase-postgres:
